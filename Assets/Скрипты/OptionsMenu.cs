
using UnityEngine;

internal class OptionsMenu : MonoBehaviour
{
    public UIVirtualButton Exit;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
